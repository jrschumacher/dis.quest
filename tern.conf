[database]
# SQLite configuration for local development
# For production, this can be overridden with environment variables
driver = sqlite
database = ./disquest.db

# For PostgreSQL in production, use:
# driver = postgres
# host = localhost
# port = 5432
# database = disquest
# user = disquest
# password = 
# sslmode = disable

[data]
# Migration settings
loadSampleData = false