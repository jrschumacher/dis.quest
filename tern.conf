[database]
# PostgreSQL configuration for local development
driver = postgres
host = localhost
port = 5432
database = disquest
user = disquest
password = disquest
sslmode = disable

# For production, override with environment variables:
# TERN_HOST, TERN_PORT, TERN_DATABASE, TERN_USER, TERN_PASSWORD

[data]
# Migration settings
loadSampleData = false

[migration]
path = ./migrations